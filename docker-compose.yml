version: '3.8'

services:
  discord-bot:
    build: .
    container_name: discord-mqtt-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Mount data directory for persistent storage
      - ./data:/app/data
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge
